(()=>{
  const PREF='nflQolV3', BETS='nflQolBetsV3';
  const readPrefs=()=>{try{return JSON.parse(localStorage.getItem(PREF)||'{}')}catch{return{}}};
  const writePrefs=p=>localStorage.setItem(PREF,JSON.stringify(p));
  const marketKey=(m,g)=>[g?.id||m.gameId||'',m.marketKey||m.type,m.player||m.team||'',m.side||'',m.point??'',m.name||''].join('|');
  const allMarkets=()=>{try{return (state.games||[]).flatMap(g=>(g.markets||[]).map(m=>({m,g,k:marketKey(m,g)})))}catch{return[]}};
  const prefLocked=()=>new Set((window.NFL_QOL?.prefs?.()||readPrefs()).locked||[]);
  const lockedForGame=g=>allMarkets().filter(x=>x.g===g&&prefLocked().has(x.k)).map(x=>x.m);
  function fillLocked(game,count,risk,variant,base){const locks=lockedForGame(game);if(!locks.length)return base;let legs=[];for(const l of locks){if(legs.length>=count)break;if(typeof coherentWithLegs!=='function'||coherentWithLegs(l,legs,variant))legs.push(l)}const pool=[...(base?.legs||[]),...(game.markets||[]).filter(m=>state.selectedMarkets?.has?.(m.type))].filter(m=>!legs.includes(m)).sort((a,b)=>(typeof candidateScore==='function'?candidateScore(b,risk,variant)-candidateScore(a,risk,variant):0));for(const m of pool){if(legs.length>=count)break;if(typeof coherentWithLegs==='function'&&!coherentWithLegs(m,legs,variant))continue;legs.push(m)}return legs.length===count&&typeof packageParlay==='function'?packageParlay(legs,variant,true,{scriptName:'Locked-leg build',thesis:'Your locked selections are held fixed and the remaining legs are chosen around them.'}):base;}
  if(typeof window.buildSgp==='function'){const orig=window.buildSgp;window.buildSgp=function(game,count,risk,variant,previous=[]){return fillLocked(game,count,risk,variant,orig(game,count,risk,variant,previous))}}
  function updateBankrollForSettlement(id,result){let bets;try{bets=JSON.parse(localStorage.getItem(BETS)||'[]')}catch{return}const b=bets.find(x=>x.id===id);if(!b||b._bankrollSettled||!['W','L','P'].includes(result))return;const input=document.getElementById('currentBankroll');if(!input)return;let bank=Number(input.value)||0;if(result==='W')bank+=b.odds>0?b.stake*b.odds/100:b.stake*100/Math.abs(b.odds);else if(result==='L')bank-=b.stake;b._bankrollSettled=true;b._settledResult=result;b.result=result;localStorage.setItem(BETS,JSON.stringify(bets));input.value=Math.max(0,bank).toFixed(2);input.dispatchEvent(new Event('input',{bubbles:true}));}
  function findMarket(k){const x=allMarkets().find(x=>x.k===k);return x?{...x.m,game:`${x.g.away} @ ${x.g.home}`,source:x.m.source||x.g.dataSource,gameContext:x.g.context||x.m.gameContext}:null}
  function ensureDrawer(){let d=document.getElementById('qWhyDrawer');if(d)return d;d=document.createElement('div');d.id='qWhyDrawer';d.innerHTML='<div class="qdrawer-backdrop" data-close></div><aside><button class="qdrawer-close" data-close>×</button><div id="qWhyContent"></div></aside>';document.body.appendChild(d);return d}
  function showWhy(m){const d=ensureDrawer(),p=Number(m.price),prob=p<0?(-p)/((-p)+100):100/(p+100),warnings=[];if(m.type==='td')warnings.push('Touchdown markets are inherently high variance.');if(Math.abs(p)>200)warnings.push('The price is outside the preferred core value band.');if(m.source==='Kalshi')warnings.push('Kalshi is an implied-price proxy; compare the final line at Caesars before betting.');const info=window.NFL_CONFIDENCE?.explain?.(m)||{},conf=info.score||0,factors=(info.contextFactors||[]).join(' • ');document.getElementById('qWhyContent').innerHTML=`<span class="qdrawer-kicker">WHY / WHY NOT</span><h2>${m.name}</h2><p>${m.game||''} • ${m.source||'Live'}</p><div class="qdrawer-grid"><div><small>Price</small><b>${p>0?'+':''}${p}</b></div><div><small>Implied</small><b>${(prob*100).toFixed(1)}%</b></div><div><small>Confidence</small><b>${conf||'—'}</b></div></div><h3>Context signals</h3><p>${factors||'No material contextual adjustment was available for this market.'}</p><h3>Before placing it</h3><p>Verify the current Caesars line before betting.</p>`;d.classList.add('open')}
  document.addEventListener('click',e=>{const btn=e.target.closest('button');if(!btn)return;if(btn.dataset.r){const row=btn.closest('[data-id]');if(row)updateBankrollForSettlement(row.dataset.id,btn.dataset.r)}if(btn.dataset.act==='why'){const k=btn.closest('[data-k]')?.dataset.k,m=k&&findMarket(k);if(m){e.preventDefault();e.stopImmediatePropagation();showWhy(m)}}if(btn.hasAttribute('data-close'))document.getElementById('qWhyDrawer')?.classList.remove('open');},true);
  const loadEngine=()=>{if(document.querySelector('script[data-selectivity-engine]'))return;const loadRec=()=>{const s=document.createElement('script');s.src='recommendation-engine-v2.js?v=20260915-SIMPLE1';s.dataset.selectivityEngine='1';document.body.appendChild(s)};if(window.NFL_CONFIDENCE)return loadRec();const c=document.createElement('script');c.src='confidence-engine.js?v=20260915-SIMPLE1';c.onload=loadRec;document.body.appendChild(c);};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',loadEngine,{once:true});else loadEngine();
})();